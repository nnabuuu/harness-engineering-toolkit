# EPITAPH — <skill-name>

**Born.** <Model/harness generation and date when the skill was created, and
the gap it was built to fill. One or two sentences. e.g. "Created in the
GPT-4 era to force plan-before-code discipline the model would otherwise
skip.">

**Died.** <The release event that triggered retirement: model version,
harness feature, platform merge. Date of the audit that retired it.>

**Cause of death.** <Exactly one:>
- `engineered → native (model)` — <which capability the model absorbed>
- `engineered → native (harness)` — <which platform feature replaced it>
- `superseded` — <replaced by a better skill; name it>
- `defect` — <the skill was net-negative on its own terms>

**Evidence.** <What the audit found: ablation result, trigger-cost data,
platform release notes. One or two lines. "It felt obsolete" is not
evidence.>

**Survived by.** <Where the surviving content went. List each invariant or
private constraint that was relocated, and the living skill that now holds
it. If nothing survived, write "Nothing. The entire skill was ritual." —
that sentence is itself a finding.>

---

*Interred by `/harness-skill-prune` on <date>, under <model/harness
generation>. Original files preserved unmodified in this directory.*
